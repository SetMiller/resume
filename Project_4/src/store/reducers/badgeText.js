
const initState = {
    text: [
      'React',
      'Redux',
      'Bootstrap',
      'React-route',
      'Axios',
      'Thunk (for middleware)',
    ]
}

const reducer = ( state = initState, action ) => {
  switch (action.type) {
    default: return state
  }
}
export default reducer